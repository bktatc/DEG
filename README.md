# coordinate-converter 0.1
Convert geographical coordinates from 'Degrees Minutes Seconds' to 'Decimal Degrees' in C++.

This version includs a GUI written in Qt.

To Build (Ubuntu):
$ mkdir build
$ cd build
$ cmake ..
$ cmake --build .
